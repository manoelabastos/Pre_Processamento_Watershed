Conversão de modelo tridimensional originado de uma nuvem de pontos LiDAR (.las) para um arquivo de imagem rasterizado (.tif) com 
atributos de pixel (ND) baseados no maior valor da componente Z encontrado para cada célula.
Geração de uma matriz no formato binário nativo (.npy) com mesma dimensão e resolução demarcada por pontos sementes 
previamente obtidos e organizados em um arquivo de texto (.txt).
